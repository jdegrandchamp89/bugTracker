<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>

      <div class="form-group">
        <div class="field">
          <%= form.label :lname %>
            <%= form.text_field :lname, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :fname %>
            <%= form.text_field :fname, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :email %>
            <%= form.text_field :email, :class => 'form-control' %>
        </div>

        <div class="field">
          <%= form.label :thumbnail %>
            <%= form.text_field :thumbnail, :type => 'file', :class => 'form-control-file' %>
        </div>

        <div class="actions">
          <%= form.submit :class => 'btn' %>
        </div>
      </div>
      <% end %>